const character:string = 'mario';
const list:string[]= [];
const subList:(string|number)[] = [];
console.log(character);

const inputs = document.querySelectorAll('input');

inputs.forEach(input => {
  console.log(input);
});
